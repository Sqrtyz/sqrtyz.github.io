---
title: VSC 设置
category: Others
date: 2023-09-01
---

### CMake 的使用

+ CMakelist.txt 相关

+ CMake 带命令行参数 build

    在 `.vscode` 下创建 `setting.json`，并填充以下内容：

    ```json
    {
        "cmake.debugConfig": {
            "args": [
                "arg1",
                "arg2"
            ]
        }
    }
    ```

    这会使得 run 时将 arg1 和 arg2 作为命令行参数运行。

### tasks.json 和 launch.json

个人目前的理解是，`tasks.json` 可用于配置运行脚本和启动进程，`launch.json` 可以用于配置调试选项。

使用 Ctrl + Space 显示光标位置处可以使用的属性或参数。鼠标放在属性上可以显示属性的意义。

可能有用的 VSCode 官方文档：

+ [预定义的变量 Predefined Variables](https://code.visualstudio.com/docs/editor/variables-reference)

+ [launch.json 的配置](https://code.visualstudio.com/docs/editor/debugging#_launch-configurations)

+ [tasks.json 的配置](https://code.visualstudio.com/docs/editor/tasks#vscode)

---

#### 默认生成的 tasks.json

```json
{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: gcc.exe build active file",
            "command": "C:\\mingw64\\bin\\gcc.exe", // 编译器路径
            "args": [ // 编译参数表
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": { // 可选选项
                "cwd": "${fileDirname}" // Current Working Directory
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true // 若为 true 在之后运行程序时将不再询问使用哪个编译器
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
```
---

#### 常用 launch.json

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FUCKING Launch (GDB)", // 配置名称，将会在启动配置的下拉菜单中显示
            "type": "cppdbg", // 配置类型
            "request": "launch", // 请求配置类型，可以为 launch（启动）或 attach（附加）
            "targetArchitecture": "x86", // 生成目标架构，可以为 x86, arm, arm64, mips, x64, amd64, x86_64
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe", // 将要进行调试的程序的路径
            "args": [
                "arg1",
                "arg2",
                "arg3"
            ], // 程序调试时传递给程序的命令行参数
            "cwd": "${fileDirname}", // 调试程序时的工作目录
        }
    ]
}
```

进入 VSCode 的 Debug 标签下，可以看到上方可以勾选调试的选项。